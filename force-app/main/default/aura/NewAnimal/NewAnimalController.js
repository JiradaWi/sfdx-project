({
    doInit : function(component, event, helper) {
        helper.fetchAnimalData(component);
    },
    refreshData: function(component,event, helper){
        helper.fetchAnimalData(component);
    }
})
